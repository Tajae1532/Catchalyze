// This page is no longer needed as authentication is handled at the App.tsx level
// The App.tsx component now contains all the dashboard functionality with auth integration

import React from 'react';

const Index = () => {
  return null;
};

export default Index;